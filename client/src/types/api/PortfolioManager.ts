/**
 * the portfolio manager
 */
export type PortfolioManager = {
    /**
     * the unique identifier for the portfolio manager
     */
    manager_id: number;
    name: string;
    /**
     * valid email address
     */
    email: string;
};

